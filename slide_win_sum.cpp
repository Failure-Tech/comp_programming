#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("input.tx", "r", stdin);
    int T;
    cin >> T;

    while (T--) {
        int N, K;
        cin >> N >> K;

        string s;
        cin >> s;

        vector<int> binary(N-K+1);
        // min a's, max a's
        // over each k val, 
        for (int i = K; i < N-K+1; i++) {
            
        }
    }
}