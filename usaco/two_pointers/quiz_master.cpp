#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;

    for (int test = 0; test < T; test++) {
        int N, M;
        cin >> N >> M;

        vector<int> smart(N);
        for (int i = 0; i < N; i++) {cin >> smart[i];}

        
    }
}