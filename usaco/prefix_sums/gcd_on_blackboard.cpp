#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int N;
    cin >> N;

    vector<ll> numbers(N+1);
    for (int i = 1; i <= N; i++) {
        ll x;
        cin >> x;

        numbers
    }
}